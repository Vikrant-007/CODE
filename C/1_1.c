#include<stdio.h>
int main()
{
    int l,b,area;
    printf("Enter length of rectrangle: ");
    scanf("%d",&l);
    printf("Enter breadth of rectrangle: ");
    scanf("%d",&b);
    area=l*b;
    printf("Area is: %d",area);
    return 0;
}